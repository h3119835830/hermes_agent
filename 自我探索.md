# Day1
1. 创建的agent要有的内容：
* 能够新建会话，有多个会话
* 记忆面板：围绕通用记忆和用户画像，加入当前对话专有记忆（十轮对话生成摘要）
* 支持深度思考，展示工具调用，支持对话重新生成，生成代码高亮可复制
* 可以进行联网搜索
* 可以使用终端，对当前电脑进行全自动化操作（当前已迁移至 WSL Ubuntu 环境，可原生支持完整 Bash 指令及 Linux 生态）
* **危险操作拦截审批**：Agent 在调用终端执行可能带有危险性（如删除、改权限、重启等）的命令前，会在前台弹出警告模态框，必须经过用户的“允许/拒绝”确认后才会执行
* **实时 Markdown 渲染与排版**：接入 marked.js，聊天框支持实时的富文本排版
* **动态中断控制**：支持通过前台的 🛑 按钮随时中断 Agent 后台的流式生成过程

# Day2
1. 问题：记忆机制还要自己触发它不会自动触发？
2. 上下文压缩是否可用：用他自带的
* **模型使用日志监控**：在左下角新增“使用日志”模块，点击可弹窗查看后端每次调用模型的详细指标，包括调用时间、模型名称、总耗时、首字延迟（TTFT）、输入与输出 Token 数以及是否为流式生成。
* **侧边栏会话折叠优化**：限制左侧历史会话列表仅默认显示最近的 5 条会话，超出的旧会话收纳进点击后弹出的悬浮小框内，确保 UI 布局不会因会话过多而变形。
* 添加子智能体进行审查，降低幻觉
* 识别PDF的Skill和Github的Skill

# Day3
1. 局限：目前主流的智能体产品Antigravity和CodeX等没有对外暴露API，不能通过Hermes来给MCP下发指令（无法自动化实现）
2. 正确路线：正确的做法是让 Hermes 自己长出修改代码的能力（来完善Hermes，使其能够做到精准修改代码、代码检查、自动调试等）
3. 业务闭环：业务系统 -> 调用 Hermes Web Gateway (API) -> Hermes 利用自己的工具链（配合 DeepSeek-Reasoner 等模型） -> 直接修改代码并提交 Git -> 业务系统收到回调
4. 亮点分析：
* 短期记忆（对话上下文） hermes-agent/hermes_state.py
    * 采用SQLite状态存储库（.db）
    * FTS5全文搜索：SQLite内置全文搜索引擎（迷你Elasticsearch）
    * 核心对话流：
        * 会话的建立、更新 当前会话的添加、唤醒（删除呢？）
    * 高级检索机制：
        * 根据关键词定位
        * 列出某个标签会话记录
        * 安全过滤
    * 上下文树状管理：旧会话回复或上下文太长时
        * 寻找会话最新派生后代
        * 上下文压缩后生成父子链（牛：压缩后把上一条长得要命的对话的ID填进新对话的parent_session_id字段里，然后它把那段 500 字的“前情提要”作为第一句话塞进新会话里）
    * 空间清理维护
        * 可手动删除一个Agent的某次工作记录
        * 批量修剪（比如90天以前记录一键清空）
        * 数据库聊天记录支持一键导出成json
    * 全局键值存储
        * 保留小表存一些全局的简单配置
    * 网关与特定平台桥接
        * 绑定外部交流平台
* 长期记忆（3层核心架构） memory_manager.py和memory_provider.py
    * 第一层：中枢大脑（MemoryManager）（明天看这个类）
        
    